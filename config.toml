schema_version = 3

[gateway]
require_pairing = false

[channels.telegram.main]
enabled = true
bot_token = "PASTE_YOUR_TELEGRAM_BOT_TOKEN_HERE"
allowlist = ["PASTE_YOUR_NUMERIC_TELEGRAM_ID_HERE"]

[providers.models.nvidia.nim]
kind = "nvidia"
api_key = "PASTE_YOUR_NVIDIA_NIM_API_KEY_HERE"
model = "z-ai/glm-5.2"
rate_limit_rpm = 35
max_concurrent = 1

[agents.default]
model_provider = "nvidia.nim"
mcp_bundles = ["browser"]
risk_profile = "trusted"
reply_intent_precheck = false

[risk_profiles.trusted]
level = "full"

[mcp_bundles.browser]
servers = ["render_lightpanda"]

[mcp.servers.render_lightpanda]
url = "PASTE_YOUR_LIGHTPANDA_RENDER_URL_HERE"
transport = "http"
headers = { "Content-Type" = "application/json", "Accept" = "application/json, text/event-stream" }
